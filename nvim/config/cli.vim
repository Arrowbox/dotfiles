" Set colorscheme
set background=dark

" Colorscheme setup
" See: http://chriskempson.github.io/base16/
"let base16colorspace=256
colorscheme base16-ocean

" Mouse setup to work in the terminal
set mouse=a

" Shorten timeouts on escape sequences so we can change
" cursors quickly in CLI
set timeoutlen=1000
set ttimeoutlen=10
